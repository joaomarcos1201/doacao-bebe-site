# Configuração de Produção - Use variáveis de ambiente
spring.datasource.url=${DB_URL:jdbc:sqlserver://doacao_bebe_bancodedados.mssql.somee.com:1433;databaseName=doacao_bebe_bancodedados;encrypt=true;trustServerCertificate=true}
spring.datasource.username=${DB_USERNAME:alemdopositivo}
spring.datasource.password=${DB_PASSWORD:doacaobebe}

# JWT
jwt.secret=${JWT_SECRET:minha-chave-secreta-super-segura-de-256-bits-para-jwt-tokens-em-producao}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Email
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:alemdopositivo2024@gmail.com}
spring.mail.password=${MAIL_PASSWORD:sua-senha-de-app-aqui}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Logging
logging.level.com.doacaobebe=INFO
logging.level.org.springframework.security=WARN